<Window x:Class="SimpleBookkeeping.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Entry" Width="400" Height="209.6">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="日期:" Grid.Row="0" Margin="10" />
        <DatePicker x:Name="datePicker" Grid.Row="0" Margin="80 10 10 10" SelectedDate="{Binding Date}" />
        <Label Content="详情:" Grid.Row="1" Margin="10" />
        <TextBox x:Name="descriptionTextBox" Grid.Row="1" Margin="80 10 10 10" Text="{Binding Description}" />
        <Label Content="金额:" Grid.Row="2" Margin="10" />
        <TextBox x:Name="amountTextBox" Grid.Row="2" Margin="80 10 10 10" Text="{Binding Amount}" />
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,40.8,9.6,-0.2" Width="133" Grid.RowSpan="4">
            <Button x:Name="OK" Content="确认" Click="OnOKClicked" Margin="0 10 10 10" FontSize="14" Width="60" />
            <Button x:Name="Cancel" Content="取消" Click="OnCancelClicked" Margin="0 10 10 10" FontSize="14" Width="60" />
        </StackPanel>
    </Grid>
</Window>
