<Window x:Class="SimpleBookkeeping.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Entry" Width="400" Height="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="Date:" Grid.Row="0" Margin="10" />
        <DatePicker x:Name="datePicker" Grid.Row="0" Margin="80 10 10 10" SelectedDate="{Binding Date}" />
        <Label Content="Description:" Grid.Row="1" Margin="10" />
        <TextBox x:Name="descriptionTextBox" Grid.Row="1" Margin="80 10 10 10" Text="{Binding Description}" />
        <Label Content="Amount:" Grid.Row="2" Margin="10" />
        <TextBox x:Name="amountTextBox" Grid.Row="2" Margin="80 10 10 10" Text="{Binding Amount}" />
        <StackPanel Grid.Row="5" HorizontalAlignment="Right" Orientation="Horizontal" Margin="10">
            <Button Content="OK" Click="OnOKClicked" Margin="0 10 10 10" />
            <Button Content="Cancel" Click="OnCancelClicked" Margin="0 10 10 10" />
        </StackPanel>
    </Grid>
</Window>
